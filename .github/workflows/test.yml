name: test
on:
  push:
    branches:
      - test
jobs:
  terraform:
    name: 'Test'
    runs-on: ubuntu-latest
    steps:
     - name: Clone repository
       uses: actions/checkout@v2  
      
     - name: print env
       run: |
        env
        whereis pwsh


