name: Downoad
on:
  push:
    branches:
      #- master
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/download-artifact@v3
        with:
          name: my-artifact
      - name: list artifacts
        run: ls -lr
